# 华夏拼图 - China Puzzle Game

## 项目概览
华夏拼图是一款通过完成拼图认识中国各省标志性建筑和景点的益智游戏。采用真正的拼图碎片拖拽玩法，支持三种难度级别。

## 技术栈
- 前端: 原生 HTML5 + CSS3 + JavaScript ES6+ (单页应用)
- 部署: Node.js 静态文件服务器
- 图片资源: 99 张景点图片 (JPEG格式)
- 存储: localStorage (游戏进度)

## 项目结构
```
china-puzzle/
├── public/
│   ├── index.html              # 游戏主页面（完整实现）
│   └── images/                # 99张景点图片
│       ├── 北京-天坛.jpg
│       ├── 北京-故宫.jpg
│       └── ... (共99张)
├── scripts/
│   └── dev.sh                 # 开发服务器启动脚本
├── .coze                      # 项目配置文件
└── package.json
```

## 核心功能

### 1. 拼图游戏
- **拖拽玩法**: 真正的拼图碎片拖拽交换
- **三种难度**:
  - 简单: 4×6 = 24 块
  - 中等: 6×8 = 48 块
  - 困难: 10×15 = 150 块
- **拼图判定**: 碎片正确放置时自动锁定并显示绿色边框
- **完成检测**: 所有碎片正确归位触发胜利

### 2. 景点覆盖
- **33 个省份/地区**: 北京、上海、江苏、浙江、安徽、福建、江西、山东、广东、广西、海南、四川、贵州、云南、西藏、陕西、甘肃、青海、宁夏、新疆、天津、河北、山西、内蒙古、辽宁、吉林、黑龙江、湖北、湖南、重庆、澳门、香港、台湾
- **99 张景点图片**: 每个省份 3 个标志性景点

### 3. 游戏系统
- **计时器**: 实时显示游戏用时
- **步数统计**: 记录碎片交换次数
- **金币奖励**: 通关获得金币（根据用时计算）
- **进度保存**: localStorage 持久化
- **最佳时间**: 每个关卡记录最短通关时间

### 4. 用户界面
- **省份导航**: 左右箭头切换省份
- **景点选择**: 点击切换当前省份的不同景点
- **原图预览**: 点击查看完整原图
- **难度切换**: 随时更改难度级别
- **统计面板**: 显示完成数、游戏数、最佳时间、准确率

## 启动命令

```bash
# 使用 Coze CLI (推荐)
coze dev

# 或直接运行静态服务器
bash scripts/dev.sh

# 访问地址
http://localhost:5000
```

## 游戏玩法

1. **选择省份**: 使用左右箭头或景点列表选择
2. **选择难度**: 点击简单/中等/困难切换难度
3. **开始游戏**: 点击"开始游戏"按钮
4. **拖拽拼图**: 拖动一个碎片到另一个碎片位置进行交换
5. **查看原图**: 点击"查看原图"或右侧缩略图
6. **完成挑战**: 所有碎片正确归位即通关

## 关键代码逻辑

### 拼图碎片交换
```javascript
// 拖拽碎片后，交换两个碎片的位置
function swapPieces(piece1, piece2) {
  // 交换 displayIndex
  // 更新 DOM 位置
  // 检查是否正确放置
  // 增加步数
}
```

### 完成检测
```javascript
// 检查所有碎片是否在正确位置
function checkWin() {
  const pieces = puzzleBoard.querySelectorAll('.puzzle-piece');
  return pieces.every((piece, idx) => 
    parseInt(piece.dataset.pieceIndex) === idx
  );
}
```

## 数据存储

localStorage 键名: `chinaPuzzleStats`
```json
{
  "coins": 100,
  "completed": 5,
  "totalGames": 10,
  "bestTimes": {
    "0_0": 120,  // 省份0_景点0的最佳时间(秒)
    "1_2": 90
  }
}
```

## 样式系统

### CSS 变量
```css
--primary: #ff6b35        /* 主色调-橙色 */
--secondary: #f7c948      /* 次要色-金色 */
--accent: #00d4aa         /* 强调色-青色 */
--bg-dark: #0a1628        /* 深色背景 */
--gold: #fbbf24           /* 金币颜色 */
--success: #22c55e        /* 成功/正确 */
```

### 拼图碎片状态
- `.dragging`: 正在拖拽
- `.drag-over`: 悬停目标
- `.correct`: 正确放置

## 注意事项
- 图片资源位于 `/images/` 目录
- 游戏进度自动保存到 localStorage
- 支持桌面和移动端触摸操作
- 碎片正确放置后显示绿色边框并隐藏编号
